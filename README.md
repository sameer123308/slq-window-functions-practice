This repository contains a structured set of 20 SQL queries focused on mastering window functions. The goal is simple: demonstrate hands-on proficiency with ranking functions, value navigation, and aggregate window operations.

What’s Covered

RANK(), DENSE_RANK(), ROW_NUMBER()

NTILE() quartile distribution

LAG() & LEAD() for time-based comparisons

SUM(), AVG(), COUNT(), MIN(), MAX() as window aggregates

FIRST_VALUE(), LAST_VALUE(), NTH_VALUE()

Running totals, regional breakdowns, and customer-level analytics



Why This Exists

This is part of my continuous learning path to strengthen SQL fundamentals for data analytics. Every query is written manually and validated in MySQL Workbench.
